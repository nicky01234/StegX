# StegX
A multi-format steganography tool built in Python that can encrypt, hide, and extract messages inside image (PNG, BMP), audio, video, and text files using a simple Linux command-line interface.
